describe("Vending Machine", () => {
  it("should not accept amount not listed in denominations", () => {});
  it("should not accept invalid currency", () => {});
  it("should not vend if money is insufficient", () => {});
});
